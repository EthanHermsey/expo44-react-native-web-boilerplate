import { StyleSheet } from 'react-native';

const GlobalStyles: StyleSheet.NamedStyles<any> = {
    center: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        textAlign: "center",
    }
}

export default GlobalStyles